---
phase: 07-build-and-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/tsup.config.ts
  - apps/worker/tsup.config.ts
  - apps/worker/src/senders/mailer.ts
autonomous: true
requirements: [BILD-01, BILD-02]

must_haves:
  truths:
    - "Running `pnpm build --filter @pleasehold/api` succeeds locally and produces apps/api/dist/index.js"
    - "Running `pnpm build --filter @pleasehold/worker` succeeds locally and produces apps/worker/dist/index.js"
    - "Starting the API or worker without SMTP_HOST logs a detailed warning naming the affected features and env vars needed"
  artifacts:
    - path: "apps/api/tsup.config.ts"
      provides: "tsup build configuration for API service"
      contains: "defineConfig"
    - path: "apps/worker/tsup.config.ts"
      provides: "tsup build configuration for worker service"
      contains: "defineConfig"
    - path: "apps/worker/src/senders/mailer.ts"
      provides: "SMTP transporter with actionable missing-config warning"
      contains: "SMTP_HOST"
  key_links:
    - from: "apps/api/package.json"
      to: "apps/api/tsup.config.ts"
      via: "build script invokes tsup which reads tsup.config.ts"
      pattern: "\"build\": \"tsup\""
    - from: "apps/worker/package.json"
      to: "apps/worker/tsup.config.ts"
      via: "build script invokes tsup which reads tsup.config.ts"
      pattern: "\"build\": \"tsup\""
---

<objective>
Fix the local build pipeline for the API and worker services by adding tsup configuration files, and improve SMTP missing-config guidance so developers know exactly what will not work and how to fix it.

Purpose: The API and worker apps reference tsup in their `build` scripts but have no tsup.config.ts, causing `pnpm build --filter @pleasehold/api` to fail locally. The current SMTP warning is a single line that does not tell developers which features break or what env vars to set.

Output: Two tsup config files (API and worker) and an improved SMTP startup warning.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/api/package.json
@apps/api/tsconfig.json
@apps/api/src/index.ts
@apps/worker/package.json
@apps/worker/tsconfig.json
@apps/worker/src/senders/mailer.ts

<interfaces>
<!-- Existing tsup configs in the monorepo to follow as patterns -->

From packages/db/tsup.config.ts:
```typescript
import { defineConfig } from 'tsup';
export default defineConfig({
  entry: ['src/index.ts'],
  format: ['esm'],
  dts: true,
  clean: true,
  sourcemap: true,
});
```

From packages/auth/tsup.config.ts:
```typescript
import { defineConfig } from 'tsup';
export default defineConfig({
  entry: ['src/index.ts', 'src/client.ts'],
  format: ['esm'],
  dts: true,
  clean: true,
  sourcemap: true,
  external: ['better-auth/react', 'better-auth/client/plugins'],
});
```

<!-- Note: API and worker are APPS not packages. They produce runnable
     artifacts (dist/index.js), NOT library exports. So:
     - dts: false (no .d.ts needed for apps)
     - No external array needed -- tsup should bundle workspace deps
       but externalize node_modules (default tsup behavior).
     - The API Dockerfile CMD is: node apps/api/dist/index.js
     - The Worker Dockerfile CMD is: node apps/worker/dist/index.js
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tsup configuration for API and worker apps</name>
  <files>apps/api/tsup.config.ts, apps/worker/tsup.config.ts</files>
  <action>
Create `apps/api/tsup.config.ts` with the following configuration:
- ABOUTME comment: explains this is the build config for the API service, produces a single bundled ESM entry point for Node.js
- entry: `['src/index.ts']`
- format: `['esm']`
- target: `'node22'` (matches Dockerfile base image and engines field)
- dts: `false` (app, not library -- no consumers need type declarations)
- clean: `true`
- sourcemap: `true`
- banner: `{ js: "import { createRequire } from 'module'; const require = createRequire(import.meta.url);" }` -- needed because some dependencies (nodemailer, ioredis, etc.) use `require()` internally. tsup ESM output needs this shim.
- noExternal: `[/@pleasehold\/.*/]` -- bundle workspace packages into the output so the runner stage only needs dist/index.js, not the full monorepo

Create `apps/worker/tsup.config.ts` with the same pattern:
- ABOUTME comment: explains this is the build config for the worker service, produces a single bundled ESM entry point
- entry: `['src/index.ts']`
- format: `['esm']`
- target: `'node22'`
- dts: `false`
- clean: `true`
- sourcemap: `true`
- banner: same createRequire shim as API
- noExternal: `[/@pleasehold\/.*/]`

Both configs follow the established tsup pattern in the monorepo but are tuned for apps (no dts, node target, workspace bundling).
  </action>
  <verify>
    <automated>cd /Users/christopher.jimenez/Src/PixelTowers/pleasehold && pnpm build --filter @pleasehold/api && test -f apps/api/dist/index.js && echo "API build OK" && pnpm build --filter @pleasehold/worker && test -f apps/worker/dist/index.js && echo "Worker build OK"</automated>
  </verify>
  <done>
- `apps/api/tsup.config.ts` exists with correct config
- `apps/worker/tsup.config.ts` exists with correct config
- `pnpm build --filter @pleasehold/api` succeeds and produces `apps/api/dist/index.js`
- `pnpm build --filter @pleasehold/worker` succeeds and produces `apps/worker/dist/index.js`
  </done>
</task>

<task type="auto">
  <name>Task 2: Improve SMTP missing-config warning with actionable guidance</name>
  <files>apps/worker/src/senders/mailer.ts</files>
  <action>
Update `apps/worker/src/senders/mailer.ts` to replace the current single-line warning with a detailed, actionable message. When SMTP_HOST is not set, the warning should:

1. State clearly which features will NOT work: email notifications and double opt-in verification emails
2. List the exact env vars needed: SMTP_HOST, SMTP_PORT (default 587), SMTP_USER, SMTP_PASS, SMTP_FROM
3. Give a one-line example of how to configure it (e.g., "Set these in your .env file or docker-compose.yml environment block")
4. Use `console.warn()` with a visible prefix like `[pleasehold]` for easy log searching

The warning should look like:

```
[pleasehold] SMTP is not configured -- the following features will not work:
  - Email notifications (channel type: email)
  - Double opt-in verification emails

  To enable email, set these environment variables:
    SMTP_HOST=your-smtp-server.com
    SMTP_PORT=587          (default: 587)
    SMTP_USER=your-username
    SMTP_PASS=your-password
    SMTP_FROM=noreply@yourdomain.com
```

Keep the transporter creation as-is (it still creates a transport even without SMTP_HOST -- this is correct "warn-only" behavior so the app starts cleanly). The warning is the fix, not blocking startup.

Preserve the ABOUTME comments. Do NOT change any other behavior in this file.
  </action>
  <verify>
    <automated>cd /Users/christopher.jimenez/Src/PixelTowers/pleasehold && grep -c "SMTP is not configured" apps/worker/src/senders/mailer.ts && grep -c "SMTP_HOST" apps/worker/src/senders/mailer.ts && grep -c "verification" apps/worker/src/senders/mailer.ts</automated>
  </verify>
  <done>
- `apps/worker/src/senders/mailer.ts` contains a multi-line warning that names affected features and lists required env vars
- The warning fires only when SMTP_HOST is missing
- Transporter creation still works without SMTP_HOST (warn-only, no crash)
  </done>
</task>

</tasks>

<verification>
1. `pnpm build --filter @pleasehold/api` succeeds locally and `apps/api/dist/index.js` exists
2. `pnpm build --filter @pleasehold/worker` succeeds locally and `apps/worker/dist/index.js` exists
3. `apps/worker/src/senders/mailer.ts` contains a detailed SMTP warning with feature list and env var instructions
4. No existing behavior changed -- only files added/updated
</verification>

<success_criteria>
- API local build works: `pnpm build --filter @pleasehold/api` exits 0 and produces dist/index.js
- Worker local build works: `pnpm build --filter @pleasehold/worker` exits 0 and produces dist/index.js
- SMTP warning is actionable: missing SMTP_HOST produces a multi-line warning naming broken features and required env vars
</success_criteria>

<output>
After completion, create `.planning/phases/07-build-and-config/07-01-SUMMARY.md`
</output>
