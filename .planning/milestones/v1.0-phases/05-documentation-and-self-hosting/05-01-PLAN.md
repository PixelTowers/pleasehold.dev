---
phase: 05-documentation-and-self-hosting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/src/openapi.ts
  - apps/api/src/routes/v1/entries.ts
  - apps/api/src/routes/v1/verify.ts
  - apps/api/src/index.ts
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "GET /doc returns a valid OpenAPI 3.0 JSON spec with paths for /api/v1/entries, /verify/:token, and /health"
    - "GET /docs renders an interactive Scalar API reference page with curl examples and response samples"
    - "The OpenAPI spec includes request body schemas, response schemas, error schemas, and API key security definition"
    - "All existing API behavior (entry submission, verification, health check) continues to work identically"
  artifacts:
    - path: "apps/api/src/openapi.ts"
      provides: "Shared OpenAPI Zod schemas for request/response/error types and reusable route definitions"
      exports: ["EntryRequestSchema", "EntryResponseSchema", "ErrorResponseSchema", "VerifyResponseSchema"]
    - path: "apps/api/src/routes/v1/entries.ts"
      provides: "Entry submission route converted from plain Hono to OpenAPIHono with createRoute"
    - path: "apps/api/src/routes/v1/verify.ts"
      provides: "Verification route converted from plain Hono to OpenAPIHono with createRoute"
    - path: "apps/api/src/index.ts"
      provides: "Main app converted to OpenAPIHono, /doc and /docs endpoints mounted"
  key_links:
    - from: "apps/api/src/index.ts"
      to: "/doc"
      via: "app.doc() OpenAPI JSON endpoint"
      pattern: "app\\.doc\\("
    - from: "apps/api/src/index.ts"
      to: "/docs"
      via: "apiReference() Scalar UI endpoint"
      pattern: "apiReference\\("
    - from: "apps/api/src/routes/v1/entries.ts"
      to: "apps/api/src/openapi.ts"
      via: "imports shared schemas"
      pattern: "from.*openapi"
---

<objective>
Convert the 3 public API routes from plain Hono to OpenAPIHono with Zod schemas, then mount an auto-generated OpenAPI JSON spec at /doc and an interactive Scalar docs UI at /docs.

Purpose: External developers need accurate, interactive API documentation to integrate with pleasehold. Auto-generating from route definitions ensures docs never drift from code.
Output: /doc serves OpenAPI 3.0 JSON, /docs serves interactive Scalar reference page, all existing routes continue working.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-and-self-hosting/05-RESEARCH.md

@apps/api/src/index.ts
@apps/api/src/routes/v1/entries.ts
@apps/api/src/routes/v1/verify.ts
@apps/api/src/middleware/api-key-auth.ts
@apps/api/src/lib/field-validator.ts
@apps/api/package.json

<interfaces>
<!-- Key types the executor needs from existing codebase -->

From apps/api/src/middleware/api-key-auth.ts:
```typescript
export type ApiKeyVariables = {
  project: typeof projects.$inferSelect & {
    fieldConfig: typeof projectFieldConfigs.$inferSelect;
  };
  apiKeyId: string;
  db: Database;
};
```

From apps/api/src/lib/field-validator.ts:
```typescript
export interface FieldConfig {
  collectName: boolean;
  collectCompany: boolean;
  collectMessage: boolean;
}
export function buildEntrySchema(fieldConfig: FieldConfig): z.ZodObject<...>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install OpenAPI dependencies and create shared schemas</name>
  <files>
    apps/api/package.json
    apps/api/src/openapi.ts
  </files>
  <action>
1. Install dependencies in the api package:
   ```bash
   pnpm add @hono/zod-openapi @scalar/hono-api-reference --filter @pleasehold/api
   ```

2. Create `apps/api/src/openapi.ts` with shared OpenAPI Zod schemas:

   - `EntryRequestSchema`: z.object with email (required, .email()), name (optional, max 200), company (optional, max 200), message (optional, max 2000), metadata (optional, record of string to string|number|boolean|null). Add `.openapi()` examples on each field. Call `.openapi('EntryRequest')` on the object.

   - `EntryResponseSchema`: z.object wrapping a `data` object with id (uuid), email (email), name (nullable string), company (nullable string), position (integer), createdAt (datetime string). Add `.openapi()` examples. Call `.openapi('EntryResponse')` on the wrapper.

   - `ErrorResponseSchema`: z.object with `error` containing code (string), message (string), and optional details array of {field, message}. Add examples. Call `.openapi('ErrorResponse')`.

   - `VerifyResponseSchema`: z.object with `data` containing verified (boolean literal true) and email (string). Call `.openapi('VerifyResponse')`.

   - Export all schemas. These are DOCUMENTATION schemas only -- the actual runtime validation in the entries handler still uses `buildEntrySchema()` from field-validator.ts because the entry schema is dynamic per project.

   NOTE: The `@hono/zod-openapi` package re-exports `z` from zod with `.openapi()` extensions. Import `{ z }` from `@hono/zod-openapi` in this file, not from plain `zod`.

   File must start with the standard 2-line ABOUTME comment.
  </action>
  <verify>
    <automated>cd /Users/christopher.jimenez/Src/PixelTowers/pleasehold && pnpm typecheck --filter @pleasehold/api</automated>
  </verify>
  <done>openapi.ts exports all 4 schema constants, api package.json includes @hono/zod-openapi and @scalar/hono-api-reference, typecheck passes</done>
</task>

<task type="auto">
  <name>Task 2: Convert routes to OpenAPIHono and mount /doc + /docs</name>
  <files>
    apps/api/src/routes/v1/entries.ts
    apps/api/src/routes/v1/verify.ts
    apps/api/src/index.ts
  </files>
  <action>
1. **Convert entries.ts** (`apps/api/src/routes/v1/entries.ts`):
   - Change `import { Hono } from 'hono'` to `import { OpenAPIHono, createRoute } from '@hono/zod-openapi'`
   - Change `const app = new Hono<{ Variables: ApiKeyVariables }>()` to `const app = new OpenAPIHono<{ Variables: ApiKeyVariables }>()`
   - Import `EntryRequestSchema`, `EntryResponseSchema`, `ErrorResponseSchema` from `../../openapi`
   - Define `submitEntryRoute` using `createRoute()`:
     - method: 'post', path: '/'
     - tags: ['Entries']
     - summary: 'Submit a waitlist or demo-booking entry'
     - description: 'Submit a new entry for the project associated with the provided API key. If double opt-in is enabled, the entry will require email verification before becoming active.'
     - security: [{ apiKey: [] }]
     - request.body.content['application/json'].schema = EntryRequestSchema
     - responses: 201 (EntryResponseSchema, 'Entry created'), 200 (EntryResponseSchema, 'Duplicate entry returned'), 400 (ErrorResponseSchema, 'Validation error'), 401 (ErrorResponseSchema, 'Invalid API key'), 429 (description only: 'Rate limit exceeded')
   - Replace `app.post('/', ...)` with `app.openapi(submitEntryRoute, async (c) => { ... })`
   - CRITICAL: Keep the handler body EXACTLY the same. The runtime validation still uses `buildEntrySchema(project.fieldConfig)` -- the OpenAPI schema is for documentation only (it shows the superset of all possible fields). The actual validation is dynamic per project.
   - Preserve the existing 2-line ABOUTME comment.

2. **Convert verify.ts** (`apps/api/src/routes/v1/verify.ts`):
   - Change `import { Hono } from 'hono'` to `import { OpenAPIHono, createRoute } from '@hono/zod-openapi'`
   - Change `const app = new Hono()` to `const app = new OpenAPIHono()` inside `createVerifyRoute`
   - Import `VerifyResponseSchema`, `ErrorResponseSchema` from `../../openapi`
   - Define `verifyTokenRoute` using `createRoute()`:
     - method: 'get', path: '/{token}'  (NOTE: OpenAPI uses `{token}` not `:token`)
     - tags: ['Verification']
     - summary: 'Verify an entry email address'
     - description: 'Verify an entry via the token sent in the double opt-in email. Each token can only be used once and expires after 48 hours.'
     - request.params = z.object({ token: z.string().uuid() })
     - responses: 200 (VerifyResponseSchema, 'Entry verified'), 400 (ErrorResponseSchema, 'Invalid or expired token')
   - Replace `app.get('/:token', ...)` with `app.openapi(verifyTokenRoute, async (c) => { ... })`
   - In the handler, change `c.req.param('token')` to `c.req.valid('param').token` (the @hono/zod-openapi validated params accessor). Actually, `c.req.param('token')` still works with OpenAPIHono, so either form is acceptable. Use `c.req.param('token')` for minimal change.
   - Keep handler body the same.
   - Preserve the existing 2-line ABOUTME comment.

3. **Convert index.ts** (`apps/api/src/index.ts`):
   - Change `import { Hono } from 'hono'` to `import { OpenAPIHono } from '@hono/zod-openapi'`
   - Add `import { apiReference } from '@scalar/hono-api-reference'`
   - Change `const app = new Hono()` to `const app = new OpenAPIHono()`
   - ALL existing route mounts, middleware, CORS, tRPC, auth, health check remain EXACTLY as they are. OpenAPIHono extends Hono, so all existing methods work.
   - AFTER the health check route and BEFORE the `serve()` call, add:

   ```typescript
   // OpenAPI JSON spec endpoint
   app.doc('/doc', {
     openapi: '3.0.0',
     info: {
       title: 'pleasehold API',
       version: '1.0.0',
       description: 'API-first waitlist and demo-booking service. Integrate with a single API key and POST request.',
     },
     servers: [
       { url: 'http://localhost:3001', description: 'Local development' },
     ],
     components: {
       securitySchemes: {
         apiKey: {
           type: 'apiKey',
           in: 'header',
           name: 'x-api-key',
           description: 'Project-scoped API key (ph_live_...)',
         },
       },
     },
   });

   // Interactive API documentation
   app.get('/docs', apiReference({
     spec: { url: '/doc' },
     pageTitle: 'pleasehold API Reference',
   }));
   ```

   - Preserve the existing 2-line ABOUTME comment at the top.
  </action>
  <verify>
    <automated>cd /Users/christopher.jimenez/Src/PixelTowers/pleasehold && pnpm typecheck --filter @pleasehold/api && pnpm build --filter @pleasehold/api</automated>
  </verify>
  <done>
    - `pnpm typecheck` passes for api package
    - `pnpm build` succeeds for api package
    - All 3 route files use OpenAPIHono instead of Hono
    - index.ts mounts /doc (JSON spec) and /docs (Scalar UI) endpoints
    - No existing behavior is broken (health, entries, verify, tRPC, auth all still mounted identically)
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck --filter @pleasehold/api` passes
2. `pnpm build --filter @pleasehold/api` produces dist/ output
3. Start the dev server (`pnpm dev --filter @pleasehold/api`) and verify:
   - `curl http://localhost:3001/health` returns `{"status":"ok"}`
   - `curl http://localhost:3001/doc` returns valid OpenAPI JSON with paths for /api/v1/entries and /verify/{token}
   - Open `http://localhost:3001/docs` in a browser to see the Scalar API reference
</verification>

<success_criteria>
- OpenAPI JSON spec auto-generated from route definitions and served at /doc
- Interactive Scalar documentation page rendered at /docs with curl examples and response samples
- All existing API routes (entries, verify, health, tRPC, auth) continue to function identically
- No manual spec maintenance required -- adding .openapi() metadata to a route automatically updates the spec
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-and-self-hosting/05-01-SUMMARY.md`
</output>
