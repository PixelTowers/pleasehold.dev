---
phase: 01-auth-projects-and-api-keys
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - biome.json
  - tsconfig.json
  - .gitignore
  - .env.example
  - packages/db/package.json
  - packages/db/tsconfig.json
  - packages/db/src/index.ts
  - packages/db/src/client.ts
  - packages/db/src/schema/index.ts
  - packages/db/src/schema/auth.ts
  - packages/db/src/schema/projects.ts
  - packages/db/src/schema/field-configs.ts
  - packages/db/src/schema/relations.ts
  - packages/db/drizzle.config.ts
  - packages/auth/package.json
  - packages/auth/tsconfig.json
  - packages/auth/src/index.ts
  - packages/auth/src/config.ts
  - packages/auth/src/client.ts
  - packages/auth/src/middleware.ts
  - packages/auth/src/types.ts
  - packages/trpc/package.json
  - packages/trpc/tsconfig.json
  - packages/trpc/src/index.ts
  - packages/trpc/src/trpc.ts
  - packages/trpc/src/context.ts
  - packages/trpc/src/router.ts
  - packages/trpc/src/routers/user.ts
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/src/index.ts
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/index.html
  - apps/web/vite.config.ts
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - apps/web/src/lib/auth-client.ts
  - apps/web/src/lib/trpc.ts
  - apps/web/src/routeTree.gen.ts
  - apps/web/src/routes/__root.tsx
  - apps/web/src/routes/index.tsx
  - apps/web/src/routes/login.tsx
  - apps/web/src/routes/signup.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "User can create an account with email and password via signup page"
    - "User can log in with email and password and be redirected to the dashboard"
    - "User session persists across browser refresh without re-authentication"
    - "User can log out from the dashboard and is redirected to login page"
  artifacts:
    - path: "packages/db/src/schema/auth.ts"
      provides: "Better Auth owned tables — auth_users, sessions, accounts, verifications"
      contains: "pgTable"
    - path: "packages/db/src/schema/projects.ts"
      provides: "Projects table with userId, name, mode columns"
      contains: "pgTable('projects'"
    - path: "packages/db/src/schema/field-configs.ts"
      provides: "Field config table with boolean toggles per project"
      contains: "pgTable('project_field_configs'"
    - path: "packages/auth/src/config.ts"
      provides: "createAuth factory with emailAndPassword enabled, apiKey plugin with ph_live_ prefix"
      contains: "emailAndPassword"
    - path: "apps/api/src/index.ts"
      provides: "Hono server with auth routes, tRPC mount, CORS, health check"
      contains: "'/api/auth/*'"
    - path: "apps/web/src/routes/login.tsx"
      provides: "Login page with email/password form"
      contains: "signIn"
    - path: "apps/web/src/routes/signup.tsx"
      provides: "Signup page with email/password form"
      contains: "signUp"
  key_links:
    - from: "apps/web/src/lib/auth-client.ts"
      to: "apps/api (Better Auth handler)"
      via: "Better Auth client configured with API base URL"
      pattern: "createAuthClient.*baseURL"
    - from: "apps/api/src/index.ts"
      to: "packages/auth/src/config.ts"
      via: "createAuth() factory call"
      pattern: "createAuth"
    - from: "packages/auth/src/config.ts"
      to: "packages/db/src/schema"
      via: "drizzleAdapter with schema references"
      pattern: "drizzleAdapter"
    - from: "apps/api/src/index.ts"
      to: "packages/trpc"
      via: "tRPC server middleware mounted on /trpc/*"
      pattern: "trpcServer"
---

<objective>
Scaffold the pleasehold monorepo from scratch, establish the database schema for all Phase 1 entities, configure Better Auth with email/password + OAuth + API key plugin, stand up the Hono API server with auth and tRPC routes, and create a minimal React dashboard with signup/login/logout — the complete foundation that every subsequent plan builds on.

Purpose: This is the identity and access layer. Without working auth, sessions, and the database schema in place, no project CRUD or API key management can be built. This plan delivers a working end-to-end auth flow (signup → login → session persistence → logout) with the monorepo structure and tooling that all future work inherits.

Output: A working monorepo with `apps/api` (Hono server), `apps/web` (React + Vite dashboard), `packages/db` (Drizzle schema + client), `packages/auth` (Better Auth config + client), `packages/trpc` (tRPC infra). User can sign up, log in, refresh and stay logged in, and log out.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-projects-and-api-keys/01-CONTEXT.md
@.planning/phases/01-auth-projects-and-api-keys/01-RESEARCH.md

Reference architecture: GoldenBerry at /Users/christopher.jimenez/Src/PixelTowers/GoldenBerry
- Copy monorepo structure (turbo.json, pnpm-workspace.yaml, biome.json patterns)
- Copy Better Auth config factory pattern from packages/auth/src/config.ts
- Copy Drizzle schema patterns from packages/db/src/schema/
- Copy tRPC context + router patterns from packages/trpc/src/
- Copy Hono server mount pattern from apps/api/src/index.ts

CRITICAL DIFFERENCES from GoldenBerry:
- Enable `emailAndPassword: { enabled: true }` (GoldenBerry disables it)
- Add `apiKey()` plugin with `prefix: 'ph_live_'` to Better Auth config
- Skip twoFactor plugin (not needed for pleasehold)
- Skip S3 and queue from tRPC context (not needed in Phase 1)
- Add projects + project_field_configs schemas (pleasehold-specific)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold monorepo and database package with complete Phase 1 schema</name>
  <files>
    package.json
    pnpm-workspace.yaml
    turbo.json
    biome.json
    tsconfig.json
    .gitignore
    .env.example
    packages/db/package.json
    packages/db/tsconfig.json
    packages/db/src/index.ts
    packages/db/src/client.ts
    packages/db/src/schema/index.ts
    packages/db/src/schema/auth.ts
    packages/db/src/schema/projects.ts
    packages/db/src/schema/field-configs.ts
    packages/db/src/schema/relations.ts
    packages/db/drizzle.config.ts
  </files>
  <action>
    **Step 1: Monorepo root**
    Create root `package.json` with `"private": true`, `"name": "pleasehold"`, `"packageManager": "pnpm@10..."`, scripts for `dev`, `build`, `lint`, `db:generate`, `db:migrate`. Reference GoldenBerry root package.json for turbo script patterns.

    Create `pnpm-workspace.yaml` with packages: `["apps/*", "packages/*"]`.

    Create `turbo.json` with pipeline for `dev`, `build`, `lint`, `typecheck` tasks. Reference GoldenBerry turbo.json. Mark `dev` as persistent.

    Create `biome.json` with formatter (indent: 2, tab) and linter config. Reference GoldenBerry biome.json.

    Create root `tsconfig.json` as base config with `strict: true`, `target: "ES2022"`, `module: "ESNext"`, `moduleResolution: "bundler"`.

    Create `.gitignore` with node_modules, dist, .env, *.local, .turbo, drizzle patterns.

    Create `.env.example` with:
    ```
    DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pleasehold
    BETTER_AUTH_SECRET=change-me-to-a-random-string
    API_URL=http://localhost:3000
    WEB_URL=http://localhost:5173
    GITHUB_CLIENT_ID=
    GITHUB_CLIENT_SECRET=
    GOOGLE_CLIENT_ID=
    GOOGLE_CLIENT_SECRET=
    ```

    **Step 2: packages/db**
    Create `packages/db/package.json` with name `@pleasehold/db`, dependencies: `drizzle-orm`, `postgres` (postgres.js driver). DevDeps: `drizzle-kit`, `typescript`.

    Create `packages/db/src/client.ts`:
    - Export `createDb(connectionUrl: string)` function that creates a postgres.js client and wraps it with `drizzle()`.
    - Pattern: `import postgres from 'postgres'; import { drizzle } from 'drizzle-orm/postgres-js';`
    - Import and pass all schema via `{ schema: allSchemas }` to drizzle.
    - Reference GoldenBerry packages/db/src/client.ts.

    Create `packages/db/src/schema/auth.ts`:
    - Define Better Auth owned tables: `authUsers`, `sessions`, `accounts`, `verifications`.
    - Reference GoldenBerry packages/db/src/schema/auth.ts for exact column definitions.
    - Also define `apikeys` table for the Better Auth apiKey plugin — columns: `id` (text PK), `name` (text), `start` (text), `prefix` (text), `key` (text, hashed), `userId` (text, FK to authUsers), `refillInterval` (text, nullable), `refillAmount` (integer, nullable), `lastRefillAt` (timestamp, nullable), `enabled` (boolean, default true), `rateLimitEnabled` (boolean, default false), `rateLimitTimeWindow` (integer, nullable), `rateLimitMax` (integer, nullable), `requestCount` (integer, default 0), `remaining` (integer, nullable), `metadata` (text, nullable — stores JSON string with projectId), `expiresAt` (timestamp, nullable), `createdAt` (timestamp), `updatedAt` (timestamp), `permissions` (text, nullable), `deletedAt` (timestamp, nullable).
    - Do NOT add custom columns to these tables. They are owned by Better Auth.

    Create `packages/db/src/schema/projects.ts`:
    - `projects` table: `id` (uuid PK, defaultRandom), `userId` (text, NOT NULL, FK → authUsers.id, onDelete cascade), `name` (text, NOT NULL), `mode` (text enum ['waitlist', 'demo-booking'], NOT NULL), `createdAt` (timestamp with timezone, defaultNow), `updatedAt` (timestamp with timezone, defaultNow).
    - Add index on `userId`: `index('projects_user_id_idx').on(table.userId)`.
    - Note: `userId` is `text` type to match Better Auth's authUsers.id (which is text, not uuid).
    - Add comment noting mode is immutable after creation — enforced at application layer.

    Create `packages/db/src/schema/field-configs.ts`:
    - `projectFieldConfigs` table: `id` (uuid PK, defaultRandom), `projectId` (uuid, NOT NULL, unique, FK → projects.id, onDelete cascade), `collectName` (boolean, NOT NULL, default false), `collectCompany` (boolean, NOT NULL, default false), `collectMessage` (boolean, NOT NULL, default false), `createdAt` (timestamp with timezone, defaultNow), `updatedAt` (timestamp with timezone, defaultNow).
    - Email is always required — no toggle needed, enforced at API layer.
    - Seed defaults set based on mode at project creation time (in tRPC mutation, Plan 02).

    Create `packages/db/src/schema/relations.ts`:
    - Drizzle relations: authUsers → projects (one-to-many), projects → projectFieldConfigs (one-to-one), authUsers → apikeys (one-to-many), projects has no direct relation to apikeys (scoping via metadata).

    Create `packages/db/src/schema/index.ts` barrel export of all schema files.
    Create `packages/db/src/index.ts` barrel export of client + schema.

    Create `packages/db/drizzle.config.ts` pointing to schema directory, PostgreSQL dialect, `DATABASE_URL` env var for connection.

    **Do NOT run migrations yet** — schema must be reviewed first. Migrations will be generated and run as part of verification.
  </action>
  <verify>
    <automated>cd packages/db && npx tsc --noEmit 2>&1 | head -30</automated>
    Verify: TypeScript compiles with no errors. All schema files export their tables. drizzle.config.ts is valid.
  </verify>
  <done>
    Monorepo root is configured with turbo, pnpm workspaces, biome. packages/db has complete Drizzle schema for auth tables (authUsers, sessions, accounts, verifications, apikeys), projects table, and projectFieldConfigs table. All FK relationships defined. createDb factory exported.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create auth package, tRPC infrastructure, Hono API server, and React web shell with auth flow</name>
  <files>
    packages/auth/package.json
    packages/auth/tsconfig.json
    packages/auth/src/index.ts
    packages/auth/src/config.ts
    packages/auth/src/client.ts
    packages/auth/src/middleware.ts
    packages/auth/src/types.ts
    packages/trpc/package.json
    packages/trpc/tsconfig.json
    packages/trpc/src/index.ts
    packages/trpc/src/trpc.ts
    packages/trpc/src/context.ts
    packages/trpc/src/router.ts
    packages/trpc/src/routers/user.ts
    apps/api/package.json
    apps/api/tsconfig.json
    apps/api/src/index.ts
    apps/web/package.json
    apps/web/tsconfig.json
    apps/web/index.html
    apps/web/vite.config.ts
    apps/web/src/main.tsx
    apps/web/src/App.tsx
    apps/web/src/lib/auth-client.ts
    apps/web/src/lib/trpc.ts
    apps/web/src/routeTree.gen.ts
    apps/web/src/routes/__root.tsx
    apps/web/src/routes/index.tsx
    apps/web/src/routes/login.tsx
    apps/web/src/routes/signup.tsx
  </files>
  <action>
    **Step 1: packages/auth**
    Create `packages/auth/src/config.ts`:
    - Export `createAuth(options: AuthOptions)` factory function.
    - `AuthOptions`: `{ db, secret, baseUrl, trustedOrigins, onUserCreated? }`.
    - Inside, call `betterAuth({...})` with:
      - `database: drizzleAdapter(options.db, { provider: 'pg', schema: { user: authUsers, session: sessions, account: accounts, verification: verifications, apikey: apikeys } })`
      - `secret: options.secret`
      - `baseURL: options.baseUrl`
      - `trustedOrigins: options.trustedOrigins`
      - `emailAndPassword: { enabled: true }` — CRITICAL: GoldenBerry disables this, pleasehold needs it
      - `session: { expiresIn: 60 * 60 * 24 * 7, updateAge: 60 * 60 * 24 }` (7 days, 1 day)
      - `socialProviders`: GitHub + Google, only if env vars present (check `process.env.GITHUB_CLIENT_ID` etc.)
      - `plugins: [apiKey({ prefix: 'ph_live_' })]`
      - `databaseHooks.user.create.after`: calls `options.onUserCreated?.({...})` — this hook is for future user sync, can be a no-op for now
    - Reference GoldenBerry packages/auth/src/config.ts closely.

    Create `packages/auth/src/client.ts`:
    - Export `createAuthClient(options: { baseURL: string })` that returns `betterAuthClient({ baseURL, plugins: [apiKeyClient()] })`.
    - Import from `better-auth/client` and `better-auth/client/plugins`.

    Create `packages/auth/src/middleware.ts`:
    - Export a Hono middleware function `authMiddleware(auth)` that extracts session from request headers via `auth.api.getSession({ headers: c.req.raw.headers })` and sets it on `c.set('session', ...)` and `c.set('user', ...)`.
    - This is optional middleware for protected Hono routes (tRPC has its own session extraction in context).

    Create `packages/auth/src/types.ts`:
    - Export `AuthUser` and `AuthSession` interfaces matching Better Auth's return shapes.
    - Export `AuthOptions` type used by createAuth.

    Create `packages/auth/src/index.ts` barrel export.
    Create package.json and tsconfig.json.

    **Step 2: packages/trpc**
    Create `packages/trpc/src/trpc.ts`:
    - `initTRPC` with superjson transformer.
    - Export `publicProcedure` (no auth check).
    - Export `protectedProcedure` with middleware that checks `ctx.session` and `ctx.user`, throws `UNAUTHORIZED` if missing.
    - Reference GoldenBerry packages/trpc/src/trpc.ts.

    Create `packages/trpc/src/context.ts`:
    - Export `Context` interface: `{ db, session, user, auth, requestHeaders }`.
    - `db`: Database type from @pleasehold/db.
    - `session`: AuthSession | null.
    - `user`: AuthUser | null.
    - `auth`: ReturnType of createAuth (needed for apiKey operations in Plan 03).
    - `requestHeaders`: Headers (needed for Better Auth API calls from tRPC mutations).
    - Export `createContext(opts)` factory.

    Create `packages/trpc/src/routers/user.ts`:
    - `user.me` protectedProcedure query — returns `ctx.user` (simple user info).

    Create `packages/trpc/src/router.ts`:
    - `appRouter` merging `userRouter`. Project and apiKey routers will be added in Plans 02 and 03.
    - Export `AppRouter` type.

    Create barrel exports, package.json, tsconfig.json.

    **Step 3: apps/api**
    Create `apps/api/src/index.ts`:
    - Hono app with:
      - CORS middleware allowing `process.env.WEB_URL ?? 'http://localhost:5173'`, credentials: true.
      - `app.all('/api/auth/*', ...)` — Better Auth handler. Reference GoldenBerry apps/api/src/index.ts.
      - `app.use('/trpc/*', trpcServer(...))` — tRPC mounted with createContext that extracts session via `auth.api.getSession({ headers: c.req.raw.headers })`.
      - `app.get('/health', ...)` — returns `{ status: 'ok' }`.
      - Comment placeholder for `// Public REST API (API-key authenticated) — added in Phase 2` at `/api/v1/*`.
    - Initialize: `createDb(DATABASE_URL)`, `createAuth({ db, secret, baseUrl, trustedOrigins })`, then mount routes.
    - Serve with `@hono/node-server` serve function on `PORT ?? 3000`.

    Create package.json with deps: hono, @hono/node-server, @hono/trpc-server, @pleasehold/db, @pleasehold/auth, @pleasehold/trpc. Dev script: `tsx watch src/index.ts`.

    **Step 4: apps/web**
    Create React 19 + Vite app with TanStack Router:
    - `vite.config.ts`: React plugin, TanStack Router plugin, proxy `/api` and `/trpc` to `http://localhost:3000`.
    - `src/lib/auth-client.ts`: `createAuthClient({ baseURL: '/api/auth' })` — proxy handles routing to API server.
    - `src/lib/trpc.ts`: tRPC client + React Query setup. `httpBatchLink` with URL `/trpc`. Include `credentials: 'include'` for cookie auth.
    - `src/App.tsx`: TanStack Router provider + tRPC provider + QueryClient provider.
    - `src/routes/__root.tsx`: Root layout with nav bar. If user is logged in (use `authClient.useSession()`), show dashboard link + logout button. If not, show login/signup links. AUTH-03: Logout button calls `authClient.signOut()` then navigates to `/login`.
    - `src/routes/index.tsx`: Dashboard index route (protected). If not authenticated, redirect to `/login`. For now, show "Welcome to pleasehold" with user email. This page will be enhanced with project list in Plan 02.
    - `src/routes/login.tsx`: Login form with email + password fields. On submit, call `authClient.signIn.email({ email, password })`. On success, redirect to `/`. Show error message on failure. Link to signup page.
    - `src/routes/signup.tsx`: Signup form with name, email, password fields. On submit, call `authClient.signUp.email({ name, email, password })`. On success, redirect to `/`. Show error message on failure. Link to login page.

    Keep styling minimal — use basic CSS or inline styles. Dashboard styling is Claude's discretion per CONTEXT.md. Focus on functional correctness, not visual polish.

    **CRITICAL ANTI-PATTERNS TO AVOID:**
    - Do NOT modify Better Auth owned tables (auth_users, sessions, accounts, verifications, apikeys).
    - Do NOT use `jsonwebtoken` or custom session handling — Better Auth manages sessions.
    - Do NOT forget `trustedOrigins` in Better Auth config — causes CORS failures on auth endpoints.
    - Do NOT forget `credentials: 'include'` on tRPC client — cookies won't be sent without it.
  </action>
  <verify>
    <automated>pnpm install && pnpm turbo typecheck 2>&1 | tail -30</automated>
    Verify: All packages install successfully. TypeScript compiles across all packages and apps with no errors.
    Then verify auth flow manually: start API + web (`pnpm dev`), navigate to /signup, create account, verify redirect to /, refresh page (session persists), click logout, verify redirect to /login.
  </verify>
  <done>
    Complete monorepo running: API server on :3000 with Better Auth routes (/api/auth/*) and tRPC (/trpc/*). Web dashboard on :5173 with login, signup, and protected index route. User can sign up with email/password (AUTH-01), session persists across refresh (AUTH-02), and logout works from any page (AUTH-03). GitHub/Google OAuth configured but env-gated (optional).
  </done>
</task>

</tasks>

<verification>
**End-to-end auth flow verification:**
1. `pnpm dev` starts both API server (:3000) and web app (:5173)
2. Navigate to http://localhost:5173/signup → signup form renders
3. Enter name, email, password → submit → redirected to dashboard (/)
4. Dashboard shows user email → session is active
5. Refresh browser → still on dashboard, still shows user email (AUTH-02: session persists)
6. Click logout → redirected to /login (AUTH-03: logout from any page)
7. Try navigating to / → redirected to /login (session is gone)
8. Navigate to /login → login with same credentials → redirected to dashboard

**Database verification:**
- `drizzle-kit generate` produces migration files without errors
- `drizzle-kit migrate` applies schema to PostgreSQL
- auth_users, sessions, accounts, verifications, apikeys, projects, project_field_configs tables exist

**API verification:**
- `curl http://localhost:3000/health` returns `{"status":"ok"}`
- `curl http://localhost:3000/api/auth/ok` returns Better Auth health check
</verification>

<success_criteria>
- Monorepo structure matches: apps/api, apps/web, packages/db, packages/auth, packages/trpc
- `pnpm install` succeeds with no peer dependency errors
- `pnpm turbo typecheck` passes across all packages
- Better Auth handles signup, login, session, logout end-to-end
- Database schema includes all Phase 1 tables (auth + projects + field_configs + apikeys)
- tRPC is mounted and responsive (user.me returns current user)
- Web app has working routes: /login, /signup, / (protected)
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-projects-and-api-keys/01-01-SUMMARY.md`
</output>
